services:
  convertx:
    image: ghcr.io/c4illin/convertx:latest
    container_name: convertx
    labels:
      yantr.app: "convertx"
      yantr.service: "ConvertX"
      yantr.info: "Self-hosted file conversion tool supporting multiple formats"
    ports:
      - "3000"
    restart: unless-stopped
