services:
  snort:
    image: voidic/snort:v0.3.0
    container_name: snort
    labels:
      yantr.app: "snort"
      yantr.service: "Fast and lightweight Nostr web client built with React for easy network interaction"
    ports:
      - "8080"
    init: true
    restart: unless-stopped
