services:
  ttyd-ubuntu:
    image: tsl0922/ttyd:latest
    container_name: ttyd-ubuntu
    labels:
      yantr.app: "ttyd-ubuntu"
      yantr.service: "Share your terminal over the web — runs a bash shell in Ubuntu Linux accessible from any browser"
    ports:
      - "7681"
    restart: unless-stopped
